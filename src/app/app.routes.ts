import { Routes } from '@angular/router';
import { StorePageComponent } from './features/store-page/store-page.component';

export const routes: Routes = [
    {path:'/', component: StorePageComponent}
];
